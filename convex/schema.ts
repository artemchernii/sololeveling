import { defineSchema } from 'convex/server'

/* The real schema lands in Phase 1 (PLAN.md §2). Phase 0 ships an empty one so
   the generated types exist and `convex dev` runs. */
export default defineSchema({})
